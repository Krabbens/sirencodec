# Environments & caches
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Caveman-compress backups (full prose before token squeeze)
*.original.md

# Local scratch / one-off plots (not part of source)
/spectrogram_comparison.png
installed_packages.txt

# IDE / local
.cursor/
.idea/
*.swp

# Data & large local assets (keep manifests out or use LFS explicitly)
data/

# Build
dist/
build/
*.egg-info/

# Root-level training TSV dumps (results.tsv is tracked)
/log.tsv
/log_*.tsv

# Root-only checkpoint dirs (do not use bare `checkpoints/` — that ignores experiments/**/checkpoints too)
/checkpoints/
/checkpoints_*/
# TensorBoard event dirs (train_mlx --tensorboard-dir)
/tensorboard_logs/

# Local experiment runs (optional; not part of source tree)
/experiments/
*.log
pca_components.pt
test_original.wav
.agents/
.qwen/

# Bundled upstream clone (nested .git); use submodule or copy separately if needed
LuxTTS/
