# Legacy material

This directory preserves files that belonged to the historical root layout of
`Q_unsmooth`. They are retained for experiment provenance and thesis audit,
but they are not active package entrypoints.

Files under `root-snapshot/` keep their previous relative layout whenever
possible. Run them only when reproducing the historical environment described
by this branch manifest. Generated datasets, checkpoints and audio remain
external to Git.
