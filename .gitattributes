# Git LFS — large binaries (install: git lfs install)
# Note: experiments/**/checkpoints/*.pt is gitignored; other *.pt (if tracked) use LFS.
*.pt filter=lfs diff=lfs merge=lfs -text
*.npy filter=lfs diff=lfs merge=lfs -text
experiments/**/spectrograms/*.png filter=lfs diff=lfs merge=lfs -text
